"""TEE UEFN (Phase 12): the Fortnite/Verse module (A22-A23).

Core is docs+codegen: Verse API digests parsed from the USER'S LOCAL
install into version-keyed facts (digests are Epic-copyrighted - never
redistributed; tests use a synthetic fixture), digest diffing as the
version firewall, offline symbol linting that kills the dominant
hallucination classes, and the Blender export lane nobody else has
built. The live editor surface is a thin capability-probed proxy of
Epic's own UEFN MCP toolsets - never a from-scratch bridge.
"""
