"""The pipeline lane (A43 P0+): declared build and query steps for ANY
project, run as the trust kernel's first tenant.

Declaration - not scene state - is what makes work drivable, so a project
says what its steps are in its own tracked `.tee/pipeline.toml` and TEE
contributes execution discipline rather than domain knowledge.
"""
