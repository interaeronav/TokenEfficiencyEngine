"""TEE KB: read-only query access to the Expert Knowledge Base (A31).

The corpus is imported reference (A30): everything served from here carries
its own confidence / jurisdiction / status flags verbatim, and grounds
nothing until re-checked against the source its frontmatter cites.
"""
