"""TEE Design (Phase 10): the expert game-design module.

Evidence-backed reference tables (every figure: value + source + as_of),
the machine-verifiable `tee-design/1` spec as the source of truth (prose
is a rendered view), and a cost-ordered verification battery - the
LLM-proposes / formal-system-verifies pairing (A16-A18).
"""
