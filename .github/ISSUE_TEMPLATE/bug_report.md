---
name: Bug report
about: Something answered wrong, hung, or cost more tokens than it should
labels: bug
---

**What happened**

**What you expected**

**`tee doctor` output** (it names most environment problems and the fix)

```
paste here
```

**Versions**: `tee --version`, OS, DCC (Blender/UE) version if involved.

**Smallest reproduction** — ideally the exact tool call(s) and the
response(s). If a response was oversized or a scene dump leaked through,
paste the whole response; token waste is a bug here.
