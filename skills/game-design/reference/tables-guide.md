# Reference tables: what each answers

All served by `gd_benchmark` / `gd_genre`; raw files under
`server/src/tee/design/data/` — every figure carries source + as_of.

| table | answers | headline anchors |
|---|---|---|
| benchmarks | "what is a good D7 for mobile puzzle" | mobile D1/D7/D30 median 22/4/0.7% (GameAnalytics 2026); PC 7/1.2/0.2%; folk 30/15/8 = top decile; wishlists→week1 ×0.15; EA launch IS the launch |
| genres | conventions, price bands, hit rates, avoid-list | survival-craft 20.8% hit rate (best); 3D co-op $8–25 (best-evidenced niche); platformer 0.18% (avoid); F2P live-service graveyard |
| motivations | audience vectors + encoded findings | 12 dims, 6 pairs, 3 clusters; age > 2× gender's variance; social ties = strongest late retention |
| ux_params | the ENFORCE table + judge notes | subtitles ≥46 px default-on; ≤3 flashes/s; input <100 ms; remapping required; defaults dominate (95% vs 50% subtitle usage) |
| economy_archetypes | sink/faucet bands, curve math, scope weights | Δsupply = Σfaucets − Σsinks; RuneScape 2^(n/7); idle r 1.07–1.15; Genshin pity params |
| dark_patterns | the code-severity rulebook | Belgium loot-box ban; Brazil U18 Mar 2026; FTC $245M; EU CPC price display; streak grace mandatory |

Licensing rules baked into the tables: aggregate findings and
paraphrased constructs only; PXI/miniPXI is the instrument default, GEQ
is banned (never validated), PENS is proprietary (never bundle); no bulk
extraction from commercial reports (EU database right); machinations.io
free-tier diagrams are public-by-default (warn users before putting
designs there).
