# A41 build script — the merged campaign: router (A39) × reality capture (A40)

**This script REPLACES `CLAUDE_A39_SCRIPT.md` and `CLAUDE_A40_SCRIPT.md`**
(owner directive, 2026-08-29: integrate all proposed changes into one
script). Designs of record: research 55 (+ swap addendum), 56 (+ DJI
addenda), and 57 (the integration map — read it FIRST; the seams and
the law union live there). Phase content marked "= A39 …" / "= A40 …"
is UNCHANGED from its source script — read the source section when
working that phase; this script owns only the order and the seams.

A one-paste prompt for a fresh session:

> Read CLAUDE.md, then CLAUDE_A41_SCRIPT.md, then research docs 57,
> 55 and 56, then the last dated entries of docs/PROGRESS.md. Call
> tee_status and tee_recall first and use TEE's own tools as co-pilot
> throughout. Work the phases in order from where the evidence says
> they stand — probes before lanes, the capture protocol before the
> site visit, fakes before live, verifiers before confidence,
> benchmarks before claims. Stop and report if any phase's premise no
> longer holds.

## Laws (the union — research 57; no relaxations)

Surface LAW 2,028/17, everything ships virtual. The owner decides
design truth; the site datum is immutable; honesty bands are
metadata-driven. TEE never calls cloud APIs; TEE/Q pin suspends
roaming; swap authority stands WITH the merged extension: **one
machine-load ledger** — active reconstruction jobs are residents the
router's memory guard respects, and reconstruction launch respects
model residency in return. Applies checkpointed; uncalibrated
confidence gates nothing; bars are the floor; never concurrent with
any other campaign; the trip is never the first test.

## The spine

### T0 — Installs + probes (= A40 V0, unchanged)
Batched owner ask (CloudCompare, QGIS ~1.3 GB, ODM arm64 image, the
PhotogrammetrySession helper, QGIS MCP plugin), the probe table —
incl. the Mini-FAMILY coverage probe (owner adjustment 2026-08-29:
no model interrogation; every set resolves from its own metadata) —
KB `rebuild.py` hygiene, disk stated.

### T1 — The capture protocol (= A40 V1, unchanged; THE deadline deliverable)
`docs/okongo-capture-protocol.md` with the DJI section and the
per-aircraft appendix. Ships before any router work is touched.

### R0 — The routing dataset (= A39 R0, unchanged)
May interleave with T2's long jobs from here on: router phases run
while reconstructions occupy the machine — the merged campaign makes
that legal within one branch.

### T2 — Ingest + reconstruct (= A40 V2, unchanged)
The DJI-spectrum resolver (four fixture paths), the helper's measured
quality ladder, ODM with metadata-matched correction.

### R1 — The cascade on fakes (= A39 R1) + THE GUARD SEAM (new, the merge's core)
The verifier-gated cascade as written, PLUS: the memory guard gains
job-class awareness — reconstruction jobs register in the machine-load
ledger; a routed swap is refused (with the honest line) while a
registered job runs; a reconstruction launch reports what is resident.
Fixtures BOTH directions (swap refused during a fake job; job launch
noting a resident 27B). Acceptance: cascade contract green on fakes +
the two seam fixtures.

### T3 — Georeference + align (= A40 V3, unchanged)

### T4 — The deviation engine (= A40 V4, unchanged) + routed chores
Once R1 is green, the lane's chore-shaped work (set-QA captions,
deviation-fact phrasing, kb lookups) rides the cascade with its
verifiers; before that, static — the lane never waits on the router.

### R2 — Accounting (= A39 R2) + the merged meter
One meter: escalation rate, swap count/seconds AND job-class
occupancy visible together; provenance carries engine + verifier
verdict + what was resident.

### T5 — Apply lanes (= A40 V5, unchanged)

### R3 — Calibration or static (= A39 R3, unchanged)

### T6 — The dry run (= A40 V6, unchanged) — also R4's workload source
The full pipeline on existing Dropbox site imagery; its real chores
are captured (inputs + verifier outcomes) as benchmark material.

### R4 — The four-arm benchmark (= A39 R4) on real workload
Mixed-difficulty set drawn partly from T6's actual chores; the routed
arm carries its swap seconds and respects the load ledger in the same
row it must win — or the router reverts.

### CLOSE — one close-out
Full battery (all bars + the deviation-report row + the routing row);
suites + CI green; docs (setup-reality-capture, the router section of
setup-local-llm, the protocol final); artifacts rebuilt + rehearsed;
one campaign ledger with wrong-way numbers explained;
`report_savings` quoted on the closing session; `tee_remember` the
close-out incl. the pre-visit checklist state. Version by semver
(expected 0.6.0); the owner tags.
