# A42 build script — the grand campaign: router × reality capture × kernel scheduler

**This script REPLACES `CLAUDE_A41_SCRIPT.md`** (owner directive,
2026-08-29: integrate A41 and A42 into one script). Designs of
record: research 55 (+swap addendum), 56 (+DJI addenda), 57, 58 (the
scheduler), 59 (THIS merge's map — read it first; the trace prize
and the seams live there). Phase content marked "= A41 …" is
UNCHANGED from the A41 script (which itself references A39/A40
content — the reference chain is the record); K-phase content is
research 58's, amended only by the seams below.

A one-paste prompt for a fresh session:

> Read CLAUDE.md, then CLAUDE_A42_SCRIPT.md, then research docs 59,
> 58, 57, 55 and 56, then the last dated entries of docs/PROGRESS.md.
> Call tee_status and tee_recall first and use TEE's own tools as
> co-pilot throughout. Work the phases in order from where the
> evidence says they stand — probes before lanes, the capture
> protocol before the site visit, fakes before live, shadow before
> dispatch, verifiers before confidence, benchmarks before claims.
> Stop and report if any phase's premise no longer holds.

## Laws (the A41 union + research 58's scheduler laws, no relaxations)

Surface LAW 2,028/17 (the scheduler is internal machinery — zero new
always-loaded tools). Owner decides design truth; datum immutable;
honesty bands metadata-driven; applies checkpointed. TEE never calls
cloud APIs; TEE/Q pin outranks placement; swap authority with its
guards; ONE machine-load ledger. Scheduler-specific: **degrade to
static always** (kernel off or dead → today's behavior, no feature
may require it); **shadow before live; greedy before clever;
win-or-revert**; every dispatch decision logged with reason and
estimate-vs-actual. Bars are the floor; never concurrent with any
other campaign; the trip is never the first test.

## The spine (T/R = A41 unchanged; K = research 58 via the 59 seams)

### T0 — Installs + probes (= A41 T0, unchanged)
Batched owner ask, the probe table incl. the Mini-FAMILY coverage
probe, KB rebuild, disk stated.

### T1 — The capture protocol (= A41 T1; THE deadline deliverable)
Ships before anything else that could slip it.

### R0 — The routing dataset (= A41 R0)

### T2 — Ingest + reconstruct (= A41 T2)
DJI-spectrum resolver, helper quality ladder, ODM metadata-matched.

### R1 — Cascade on fakes + guard seam (= A41 R1) + SEAM: registry-form descriptors
As written, PLUS (research 59 seam 1): engine facts — profiles, job
classes, footprints — are authored directly in the K-layer registry
schema; QoS class tags annotate all work items from here on (seam 3,
labels only, no behavior).

### K0 — Descriptors, task graph, SHADOW RECORDER (the merge's core — lands NOW)
Research 58's K0, moved up: task descriptors (inputs/outputs by id,
verifier, QoS tag, cost estimate from the measured tables), the graph
substrate, and the shadow recorder — from this phase on, EVERY real
dispatch (chores, jobs, swaps, gateway calls) is recorded as a trace
alongside what the shadow scheduler WOULD have done. Zero behavior
change; the campaign starts accumulating its own Borg-style evidence.
Acceptance: traces flowing, shadow deltas visible in a dated PROGRESS
sample, overhead of recording measured and negligible.

### T3 — Georeference + align (= A41 T3)

### T4 — The deviation engine + routed chores (= A41 T4)

### R2 — Accounting (= A41 R2) + SEAM: one meter schema
The merged meter designed ONCE: escalation, swap, job-class columns
live now; the scheduler's columns (queue age, dispatch reason, shadow
delta) reserved in the same schema (seam 2).

### T5 — Apply lanes (= A41 T5)

### R3 — Calibration or static (= A41 R3)

### T6 — The dry run (= A41 T6) — recorded by the shadow layer
The full pipeline on existing Dropbox imagery; its traces join the
scheduler's evidence base (and R4's workload, as before).

### R4 — The four-arm benchmark (= A41 R4)

### ★ RELEASE GATE A — trip-ready (owner tags; recommend 0.6.0)
Capture pipeline proven on the dry run, router benchmarked, protocol
final. If the site visit arrives, THIS state travels. Full battery +
suites + artifacts rehearsed at the gate.

### K1 — QoS + admission (research 58 K1)
The R1 tags become law: admission control against the ledger,
interactive-never-behind-batch, batch yields at its checkpoints,
aging against starvation.

### K2 — Dispatch live, replay-validated (research 58 K2 + the trace prize)
The greedy cost-aware policy goes live behind config ONLY after
replaying the accumulated campaign traces (Borg/Firmament
methodology, research 59): the gate is agreement-with-or-measured-
improvement-over recorded reality. Estimate-vs-actual logged per
decision.

### K3 — Preemption + backpressure (research 58 K3)

### K4 — The mixed-load benchmark — win or revert (research 58 K4)
Arms: static (scheduler off) vs scheduled; workload = replayed real
traces from this campaign + one live mixed run (chores +
reconstruction + gateway + generation). Metrics: makespan,
interactive p95, utilization, client tokens. The scheduler earns its
existence in this row or reverts to shadow.

### CLOSE — one close-out (recommend 0.7.0)
Full battery (all bars + deviation row + routing row + the scheduler
row); suites + CI green; docs (setup-reality-capture, router section,
scheduler section with the degrade-to-static promise stated,
protocol final); artifacts rebuilt + rehearsed; one campaign ledger,
wrong-way numbers explained in place; `report_savings` quoted on the
closing session; `tee_remember` the close-out. The owner tags.
