"""A superhero in a cape of the Namibian flag: two jumps, a bouncy mat, a pool.

    python -m examples.cape_shot all --out /tmp/cape

The cape is cloth on the kernel's figure, the wind is a drag force per
triangle, the mat gives, and getting wet is a heavier fabric card with its
wetness tracked per vertex - the shot that found the Blender 5.x drift
faults now in TEE's version firewall, and the bounce that was a jump cut.
"""
