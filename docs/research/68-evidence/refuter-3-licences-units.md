{
 "claims": [
  {
   "claim": "glTF export: `SetLengthUnit_s(doc, 0.001)` + `RWGltf_CafWriter` yields a spec-correct Y-up file; P0 row 13 expects \"Y-up\", D1 handoff declares SOURCE=(Z-up, right, 0.001 m) with \"glTF identity\", P6 expects the bracket \"upright\" in Blender.",
   "refuted": true,
   "evidence": "Printed from server/.venv (OCP 7.9.3.1.1): a fresh `RWGltf_CafWriter(...).CoordinateSystemConverter()` reports `HasInputCS False, HasOutputCS True, InputLengthUnit -1.0, OutputLengthUnit 1.0`; an `RWMesh_CoordinateSystemConverter` configured exactly like the writer (output=glTF, in 0.001, out 1.0) maps (100,60,10) mm -> (0.1, 0.06, 0.01) m \u2014 NO rotation \u2014 and only after `SetInputCoordinateSystem(RWMesh_CoordinateSystem_Zup)` maps it to (0.1, 0.01, -0.06). The plan's own measured fact 4 ([0.22, 0.22, 0.012] m, Z still the thin axis) is the unrotated result. W3 fact 48: converter default -1 = no conversion, which is why cadquery rotates -90\u00b0 about X itself. `server/src/tee/assets/gltf.py:190-198` derives `dims_zup_m` by swapping y/z, so an unrotated Z-up GLB is read by the DCC adapters as [0.1, 0.01, 0.06]; Blender's importer does the same Y->Z mapping, so the plate arrives standing on edge. seamkiln's identity rule (`seamkiln/src/seamkiln/handoff.py:14-38, 97, 107-109`) is correct only because seamkiln's SOURCE is already Y-up (line 97); copying it to a Z-up kernel is the double-convert trap inverted.",
   "fix": "In `exchange/gltf.py` call `writer.ChangeCoordinateSystemConverter().SetInputCoordinateSystem(RWMesh_CoordinateSystem.RWMesh_CoordinateSystem_Zup)` alongside `SetLengthUnit_s(doc, 0.001)`. `handoff.py`: SOURCE stays (Z-up, right, 0.001) and `transform_for` returns identity for GLB only because the WRITER rotates \u2014 say so in the docstring and manifest (`why_no_transform`). Pin in tests: F1 GLB `extents_m == [0.1, 0.01, 0.06]` and `dims_zup_m == [0.1, 0.06, 0.01]`; F5 plate `[0.22, 0.012, 0.22]`; the negative fixture stays [100, 60, 10] (scale) and gains a second negative: no input CS -> `extents_m == [0.1, 0.06, 0.01]`. Correct P0 row 13's expected column and V4 step 7 (read back Blender XYZ [0.08, 0.06, 0.04] unsorted, not \"sorted\")."
  },
  {
   "claim": "P0b: an SPDX allowlist over installed metadata with \"no licence metadata = fail\" passes the planned core closure (numpy, scipy, ezdxf, trimesh) and `[brep]`.",
   "refuted": true,
   "evidence": "Printed from server/.venv: scipy 1.17.1 `License='Copyright (c) 2001-2002 Enthought, Inc. 2003, SciPy Developers.'`, `License-Expression=None`, classifier `License :: OSI Approved :: BSD License`; ezdxf 1.4.4 License=None, Expression=None, classifier MIT only; trimesh 5.0.0 `'The MIT License (MIT)'`; vtk 9.6.2 `'BSD'`; cadquery-ocp `'Apache-2.0'` as free text; cadquery `'Apache Public License 2.0'`. Only numpy, fpdf2, typing_extensions, pyparsing, pypdf, packaging carry a `License-Expression`. A literal SPDX match therefore fails scipy, ezdxf, trimesh, vtk and both OCP wheels. seamkiln's gate (`seamkiln/tests/test_licences.py:109-117`) has no allowlist at all \u2014 it substring-matches NON_COMMERCIAL_MARKERS over License+classifiers+expression (memo item 44), so nothing to copy.",
   "fix": "Specify `_spdx_of(dist)`: (1) `License-Expression` if present; (2) else Trove classifier map (`OSI Approved :: BSD License`->`BSD-3-Clause`, `MIT License`->`MIT`, `Apache Software License`->`Apache-2.0`, `Python Software Foundation License`->`PSF-2.0`, `ISC License (ISCL)`->`ISC`, `zlib/libpng`->`Zlib`); (3) else free-text `License` through a small alias table (`Apache-2.0`, `The MIT License (MIT)`, `BSD`, `Apache Public License 2.0`); fail only when all three are empty (that is the anytree/ocpsvg case -> `VERIFIED_IN_REPO`). Parse `AND`/`OR`/`WITH` with `packaging.licenses.canonicalize_license_expression` (packaging 26.3 is installed; it accepted numpy's 5-way AND and `LGPL-2.1-only WITH OCCT-exception-1.0`) and require every AND operand in the allowlist. Add two intruder/allowed fakes: classifier-only BSD must PASS, free-text `GNU Lesser General Public License v3` must FAIL."
  },
  {
   "claim": "P0b `KNOWN_PAYLOADS = {\"cadquery-ocp-novtk\": ...}` is the ONE named exception, while `[brep]` \"must accept either wheel by find_spec\" and the dev venv installs `-e <kiln>` without `[brep]`.",
   "refuted": true,
   "evidence": "Printed: `server/.venv` has `cadquery-ocp 7.9.3.1.1` (License 'Apache-2.0', requires `cadquery-ocp-proxy==7.9.3.1.1`, `vtk==9.6.2`) and `cadquery-ocp-novtk NOT INSTALLED`; PyPI today: novtk 7.9.3.1.1 Apache-2.0 requires only `cadquery-ocp-proxy==7.9.3.1.1`. W1 trap 3: both wheels carry Apache metadata over the LGPL OCCT payload, so scanners under-report. seamkiln's closure walk `seamkiln/tests/test_licences.py:90-93` silently `continue`s when a declared distribution is not installed and `_licence_text` :109-113 returns \"\" \u2014 inherited as written, the gate in the dev venv either drops the declared novtk silently or fails it as \"no metadata\", and in both cases never examines the OCP carrier actually loaded (`cadquery-ocp`), which is not in KNOWN_PAYLOADS.",
   "fix": "Key `KNOWN_PAYLOADS` on BOTH `cadquery-ocp` and `cadquery-ocp-novtk` (same tuple, same URL/date) and list `cadquery-ocp-proxy` (Apache-2.0, pure Python) as a normal allowlisted member of the `[brep]` closure. Resolve the live carrier at test time with `importlib.metadata.packages_distributions()[\"OCP\"]` and assert it is in KNOWN_PAYLOADS. A declared-but-uninstalled dependency is a `pytest.skip` naming the extra when it belongs to an extra that is not installed, and a FAIL when it is a core dependency. The NOTICE assertion runs unconditionally."
  },
  {
   "claim": "Decision 3: the prefix/name \"must not read as an Autodesk mark\", and `forgekiln`/`fg_` is offered as a candidate.",
   "refuted": true,
   "evidence": "aps.autodesk.com blog, 2022-11-28: \"The Autodesk Forge brand is evolving into Autodesk Platform Services.\" \u2014 Forge was Autodesk's developer-platform brand for a decade and the marks persist; W2 cites its successor (`aps.autodesk.com`, the `DA/` shorthand) for Design Automation, and W2's IP guardrail forbids reusing Autodesk marks as product or feature names. `docs/research/47-ai-first-gen-product.md:70` already rejected \"Shapeforge\" on a collision. The plan's `test_no_autodesk_marks_in_shipped_names` does not enumerate its mark list, so `forge` would not be caught.",
   "fix": "Strike `forgekiln`/`fg_` from Decision 3 (keep `ironkiln`/`ik_`, `boltkiln`/`bk_`). Enumerate `MARKS` in the test: `autodesk, inventor, forge, fusion, vault, nastran, anycad, ilogic, ipart, iassembly, ifeature, imate, apprentice, content center, design accelerator` \u2014 matched case-insensitively as whole tokens over the package name, tool names, verbs, entity kinds, VirtualTool descriptions/tags, pyproject name/description and `data/manifest.json`; docs stay out of scope (doc 68 must name the incumbent)."
  },
  {
   "claim": "Sheet metal: \"K default 0.44 (cited)\" / `assumed.k = \"0.44 (typical 0.3\u20130.5; BA = A\u00b7\u03c0/180\u00b7(R + K\u00b7T), Industrial Press 1994 via Wikipedia CC-BY-SA)\"`.",
   "refuted": true,
   "evidence": "W3 fact 43 \u2014 the only sheet-metal citation in the seven reports \u2014 gives the formula, OSSB/BD, and \"K typically between 0.3 and 0.5\"; the number 0.44 appears in no report. The Inventor 2026 unfold-rule page (W2's GUID-4141CA32) and sheet-metal-rule page (GUID-D186BB03), fetched today, state no numeric default. Web search today (ARKANCE UK \"Understanding the Sheet Metal K Factor in Autodesk Inventor\"; Autodesk Community): \"By default Inventor uses a K factor of K = 0.44.\" So the plan attributes the incumbent's default to Industrial Press 1994 \u2014 a false citation \u2014 while W2's guardrail says regenerate values from public standards \"with their own citations\". The arithmetic itself is right (BA 4.524 / 4.398 / 4.712; BD 3.476; flat 76.524; bend zone 9.425\u00b7W).",
   "fix": "Keep the formula citation for BA/OSSB/BD. Declare the default honestly: `assumed.k = \"0.44 \u2014 this kernel's default, inside the 0.3\u20130.5 typical range (Wikipedia 'Bending (metalworking)' CC BY-SA 4.0, citing Industrial Press 1994); no standard fixes K \u2014 pass k or a bend table for production parts\"`. Doc 68 (prose) may note it equals the incumbent's default. P5b acceptance line \"K default cited\" becomes \"K default declared with its range citation; 0.44 itself is a choice, not a citation\"."
  },
  {
   "claim": "P0b allowlist includes \"MPL-2.0 (weak copyleft, file-scoped, in-process OK \u2014 A53's CDT precedent)\" while Law 2 says OCCT is \"the ONE named weak-copyleft exception\".",
   "refuted": true,
   "evidence": "`seamkiln/pyproject.toml` `dependencies = [numpy, shapely, ezdxf, trimesh]` \u2014 no CDT and no MPL-licensed package; CDT is named only as replacement text (`seamkiln/pyproject.toml:19`, `seamkiln/tests/test_licences.py:32,34`) and as a ruling in `docs/research/67-garment-cad-lane.md:59` (\"CDT (artem-ogre) MPL-2.0 usable\"). A53 never shipped an MPL dependency, so there is no precedent, only a policy sentence; and W1's minefield table lists no MPL package in the CAD closure, so the allowlist entry admits a second weak-copyleft class generically, contradicting Law 2.",
   "fix": "Remove MPL-2.0 from the allowlist (nothing in core or `[brep]` needs it) so Law 2 is literally true. If an MPL package is ever wanted, add it as a named `KNOWN_PAYLOADS`-style row citing doc 67 \u00a72, and reword Law 2 to \"LGPL-class: OCCT only; MPL-2.0 file-scoped only by named row\"."
  },
  {
   "claim": "P0 row 16: the scipy sketch solver is checked against py-slvs \"from `server/.venv`'s `[physical]`\" with \"coordinates \u2264 1e-6 mm\" agreement.",
   "refuted": true,
   "evidence": "`server/src/tee/physical/sketch.py:5` \"Input contract (all lengths meters, angles degrees)\"; no unit scaling anywhere in the module (grep for 1000/mm/scale hits only line 5); solved coordinates are `round(x, 6)` (sketch.py, solved-points loop) -> 1e-6 m = 1e-3 mm resolution, three orders coarser than the acceptance; and the DOF convention is `free_allowance = 0 if anchored else 3` (:209-210) as the plan states. py-slvs itself (GPL-3.0, metadata printed 'Gnu General Public License 3.0') is unit-agnostic, but SolveSpace converges in solver units, so an oracle fed metres cannot certify 1e-6 mm.",
   "fix": "Drive `py_slvs` directly with millimetre values in the P0 scratch script (bypass `physical/sketch.py`), no rounding, and record SolveSpace's convergence tolerance in the PROGRESS cell; keep \u2264 1e-6 mm if attained, else pin the measured floor (e.g. 1e-4 mm) and say why. Keep the anchored-only DOF comparison and cite sketch.py:209-210 for the rotation convention."
  },
  {
   "claim": "Secondary fixtures: BenchCAD data CC-BY-4.0 and CADGenBench data ODC-BY, under the plan's header \"every licence ... claim below was verified on 2026-09-02\".",
   "refuted": true,
   "evidence": "BenchCAD: only the paper's own sentence (arxiv 2605.10865v1: released \"on Hugging Face under the CC-BY-4.0 license\", code MIT) \u2014 the HF LICENSE/card was not fetched (W4 line 69 \"(paper)\"; memo items 9, 24). CADGenBench: `raw.githubusercontent.com/huggingface/cadgenbench/main/LICENSE` = Apache-2.0 (code) and its README names `cadgenbench-data` / `cadgenbench-data-gt` HF repos with NO data licence sentence; `huggingface.co/datasets/huggingface/cadgenbench` and `.../cadgenbench-data/raw/main/README.md` return HTTP 401 to fetchers today, so \"ODC-BY\" rests on W4's reading of a card this session cannot reproduce.",
   "fix": "Add a P0b row \"fixture provenance\": read the HF dataset-card `license:` field for `BenchCAD/BenchCAD` and `huggingface/cadgenbench-data` through the browser pane or `tee_web_lookup`, paste value + URL + date into `fixtures/third_party/ATTRIBUTION.md`; extend `test_data_files_carry_provenance` to `fixtures/third_party/`; until recorded, no third-party file lands and `test_fixtures.py` runs F1\u2013F8 only. If ODC-BY is confirmed, carry its attribution line into NOTICE."
  },
  {
   "claim": "`data/materials.json` satisfies the manifest rule (source, licence, retrieved per file; loader refuses without all three) and the material numbers used in W1/W2 are correct.",
   "refuted": true,
   "evidence": "The plan names a source for every CSV (bd_warehouse Apache-2.0, threadlib BSD-3 \u2014 W1 \u00a7ISO data, W3 fact 44) but none for `materials.json`; W1: `bd_materials` \"NO LICENCE FOUND \u2014 CANNOT-SHIP\"; W3 fact 46(d): never copy engineeringtoolbox-style compilations. W2's batch masses use 7.85 g/cm\u00b3 for `steel_100cr6` (ring 19,891.0 mm\u00b3 -> 156.1 g); 100Cr6/SAE 52100 is ~7.81 g/cm\u00b3 -> 155.4 g. Housing/shaft masses (1,254.9 / 246.6 g at 7.85) and the S275 bracket 715.6 g check out arithmetically.",
   "fix": "Name the authority per value in the plan and in the file: \u03c1 = 7850 kg/m\u00b3 and E = 210,000 N/mm\u00b2 for structural steel from EN 1993-1-1 \u00a73.2.6 (design-code values \u2014 facts, not text), ReH = 275 N/mm\u00b2 (t \u2264 16) from EN 10025-2, DC01 from EN 10130, 100Cr6 \u03c1 = 7.81 from the grade datasheet class with honesty tier `datasheet`; every card is `{value, unit, source, honesty}`; the loader refuses a card without `source` exactly as it does a CSV; recompute the W2 `brg6204` note (155.4 g at 7.81)."
  },
  {
   "claim": "Measured fact 12: \"Inventor: Windows-only COM, headless = read-only Apprentice or $3/12 min cloud, no macOS, no MCP; $2,585/yr\" (and the parity numbers drawn from W2).",
   "refuted": true,
   "evidence": "Correct per W2: Windows 10/11 64-bit only, \"no official Mac compatible version\" (row F); Apprentice read-only (row F); Design Automation \"1 token or $3 for 12 Processing Minutes\" (row F); $2,585/yr (Pricing); 12 geometric constraints (row A); joint DOF Rigid 0 / Rotational 1 / Slider 1 / Cylindrical 2 / Planar 3 / Ball 3 (row C) = the plan's 0/1/1/2/3/3; 3MF unsupported (row H). Overstated: W2 row F says \"no Autodesk Inventor MCP server\" but \"Third-party MCP servers (e.g. github.com/NeonGlay/inventor-mcp, moderncad.com.au/inventor-ai) wrap the COM API on Windows\", and Inventor 2027's Autodesk Assistant is \"enabled by the Model Context Protocol\"; W2 gap 2 names a THIRD headless route the plan drops \u2014 Inventor Server / `InventorCoreConsole.exe` inside the Vault Job Processor, licence-bound to Vault automation. W2 inventories the 2026 help while 2027 ships (memo items 10, 28).",
   "fix": "Rewrite fact 12: \"Windows-only COM; headless = Apprentice (free, read-only) | Inventor Server via Vault Job Processor (licence-bound to Vault automation, no third-party products) | Design Automation ($3 per 12 processing minutes, 900 s default cap); no macOS; no Autodesk-published Inventor MCP server (Help/Fusion/Revit only) \u2014 third-party COM wrappers exist on Windows and the 2027 in-app Assistant speaks MCP but does not yet create geometry; $2,585/yr.\" Doc 68's parity matrix cites the 2026 help pages and states that 2027 is the shipping version."
  },
  {
   "claim": "Units on the wire and in exchange: STEP schema reads '' before `STEPControl_Controller.Init_s()`, then AP214IS, `SetCVal_s(\"AP242DIS\")` sticks; `write.step.unit=MM`; DXF `$INSUNITS=4` is millimetres; 3MF via trimesh writes `unit=\"millimeter\"`; `XCAFDoc_DocumentTool.SetLengthUnit_s` is bound; the fabrication-lane convention is mm.",
   "refuted": false,
   "evidence": "Printed from server/.venv (OCP 7.9.3.1.1): `CVal_s(\"write.step.schema\")` = '' before Init, 'AP214IS' after, `SetCVal_s(...,\"AP242DIS\")` -> True -> 'AP242DIS'; `write.step.unit`='MM', `xstep.cascade.unit`='MM' (W3 fact 8 agrees); `ezdxf 1.4.4 units.MM = 4`; `trimesh 5.0.0 export_3MF` present and its writer carries `unit\": \"millimeter\"` (W3 fact 14 said this only of cadquery's writer; now verified for the planned trimesh path); `XCAFDoc_DocumentTool.SetLengthUnit_s/GetLengthUnit_s` bound, value in metres (W3 fact 47); `server/src/tee/adapters/freecad/codegen.py:9-10` \"the fabrication lane speaks millimetres end to end\". A3 landscape 1190.55 \u00d7 841.89 pt and F8 \u03a3 5,204,814.21 recompute exactly.",
   "fix": "None. One wording nit: `Model(True)` lives on `STEPControl_Writer`; with `STEPCAFControl_Writer` call `ChangeWriter().Model(True)` (or set the schema before constructing the writer) and keep the `FILE_SCHEMA` assertion."
  },
  {
   "claim": "Measured fact 11's licence strings (OCCT LGPL-2.1-only + exception 1.0; cadquery-ocp/-novtk/-proxy Apache-2.0; cadquery eager casadi LGPL-3.0+ and nine VTK dylibs; build123d Apache-2.0 with unlicensed bd_materials -> pin <0.12; py-slvs GPL-3.0 no exception; fpdf2 LGPL-3.0-only; ezdxf/trimesh MIT; numpy/scipy BSD; lib3mf BSD-2; nlopt effectively LGPL-2.1+; bd_warehouse Apache-2.0 / threadlib BSD-3; FastenersWB GPL-2, BOLTS GPL-3, Wikipedia CC BY-SA never vendored) and the SPDX form `LGPL-2.1-only WITH OCCT-exception-1.0`.",
   "refuted": false,
   "evidence": "Each line matches W1 \u00a71 and the minefield table row for row (OCCT README + OCCT_LGPL_EXCEPTION.txt; PyPI JSON for the OCP wheels, casadi 3.8.0 LGPLv3+, fpdf2 `license_expression: LGPL-3.0-only`, py-slvs GPL-3.0 with SolveSpace COPYING carrying no exception; build123d dev pyproject adding bd_materials with null licence fields; lib3mf BSD-2; bd_warehouse LICENSE Apache-2.0; threadlib BSD-3; FastenersWB package.xml GPL-2.0-or-later; BOLTS GPL-3.0+; Wikipedia CC BY-SA 4.0); the nine VTK dylibs are W3 fact 50 (`otool -L`). Installed metadata printed today agrees: fpdf2 `License-Expression='LGPL-3.0-only'`, py-slvs 'Gnu General Public License 3.0', casadi 'GNU Lesser General Public License v3 or later (LGPLv3+)', nlopt 'MIT', cadquery-ocp 'Apache-2.0', numpy 'BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0'. `packaging.licenses.canonicalize_license_expression(\"LGPL-2.1-only WITH OCCT-exception-1.0\")` accepts it, so `OCCT-exception-1.0` is a valid SPDX exception id. PyPI today: cadquery-ocp-novtk 7.9.3.1.1 Apache-2.0, cp310\u2013cp314 macosx_11_0_arm64, requires only `cadquery-ocp-proxy==7.9.3.1.1`.",
   "fix": "None. Record two caveats in doc 68: nlopt's \"effective LGPL-2.1+\" is W1's inference from build flags (memo item 8) \u2014 moot because it is BANNED; and `cadquery-ocp-proxy` (Apache-2.0, pure Python) joins the `[brep]` closure and the allowlist."
  }
 ]
}