{
 "claims": [
  {
   "claim": "Measured fact 5 / D4 / risk register: after `Interface_Static.SetCVal_s(\"write.step.schema\",\"AP242DIS\")`, `STEPControl_Writer.Model(True)` MUST follow or the file stays AP214; the CAF route is `STEPCAFControl_Writer` (name/colour/layer modes) -> `Model(True)`.",
   "refuted": true,
   "evidence": "PYTHONDONTWRITEBYTECODE=1 server/.venv/bin/python runs (box written to a TemporaryDirectory, FILE_SCHEMA grepped): A `STEPControl_Writer()` -> SetCVal AP242DIS -> Transfer -> Write, NO Model(True) = `AP242_MANAGED_MODEL_BASED_3D_ENGINEERING_MIM_LF`; C static set before ctor = AP242; D and F `STEPCAFControl_Writer()` with no Model call at all = AP242; I ctor -> `Model()` -> SetCVal -> Transfer = AP242 (schema is captured at the first `Transfer`, not at model creation); ONLY G ctor -> Transfer -> SetCVal -> Write stays `AUTOMOTIVE_DESIGN {1 0 10303 214 ...}` and H `Model(True)` + re-Transfer fixes it. `hasattr(STEPCAFControl_Writer,\"Model\") == False` (the plan names a method that does not exist on that class; its STEPControl_Writer is reached via `ChangeWriter()`/`Writer()`). Also verified: `CVal_s(\"write.step.schema\")` is `''` before `STEPControl_Controller.Init_s()` and `AP214IS` after; `write.step.unit` `''` -> `MM`; `SetCVal_s(...,\"AP242DIS\")` -> True but `SetCVal_s(...,\"AP242\")` -> False (only AP203/AP214CD/AP214DIS/AP214IS/AP242DIS are accepted).",
   "fix": "Rewrite the STEP rule as: call `STEPControl_Controller.Init_s()`, set `write.step.schema` to exactly `AP242DIS` BEFORE the writer's first `Transfer`; `Model(True)` is needed only to reset a writer that has already transferred under the old schema. CAF route: `w = STEPCAFControl_Writer(); w.SetNameMode(True); w.SetColorMode(True); w.SetLayerMode(True); (if the writer is reused) w.ChangeWriter().Model(True); w.Transfer(doc, STEPControl_AsIs); w.Write(path)`. Keep the `FILE_SCHEMA contains AP242` assertion and add a negative test for ordering G (Transfer-then-SetCVal) so the real trap is pinned."
  },
  {
   "claim": "Measured fact 2, \u00a7V3 F1 'front HLR 8/9', P0 row 8 and P5a acceptance: `HLRBRep_Algo` front view of F1 (100x60x10 plate, \u00d810 at (50,30)) yields 8 visible / 9 hidden edges.",
   "refuted": true,
   "evidence": "F1 built as Box(100,60,10) - Cylinder(r5 at (50,30), z -1..11): 7 faces, 59,214.602 mm3 (matches the plan). `HLRBRep_Algo().Add(f1); Projector(HLRAlgo_Projector(gp_Ax2(gp_Pnt(0,0,0), gp_Dir(d)))); Update(); Hide()`, then `TopExp_Explorer(TopAbs_EDGE)` per `HLRBRep_HLRToShape` compound: d=(0,-1,0) and (0,1,0): VCompound 4, Rg1LineV 0, OutLineV 0 | HCompound 9, Rg1LineH 0, OutLineH 1 -> visible 4 / hidden 10; d=\u00b1X: 4 / 12 (H 10 + OutLineH 2); d=\u00b1Z: 5 / 5; iso (1,1,1): 12 / 8; cadquery iso (-1.75,1.1,5): 14 / 8; without `Hide()`: VCompound 13 + OutLineV 1, hidden 0. No projector or compound sum reproduces 8/9. First front call 2.1 ms, warm 0.3 ms.",
   "fix": "Replace the 8/9 pin with per-compound counts measured under a NAMED projector (origin, direction, Hide() called): front `gp_Dir(0,-1,0)`: VCompound 4 | HCompound 9 + OutLineHCompound 1; top `gp_Dir(0,0,-1)`: 5 | 5; right `gp_Dir(1,0,0)`: 4 | HCompound 10 + OutLineH 2. Make P0 row 8 record all ten compounds separately plus the projector, and define `vw:*` `visible_edges` = V+Rg1LineV+OutLineV and `hidden_edges` = H+Rg1LineH+OutLineH (build123d's convention) so the numbers in `tests/expected.py` are derivable."
  },
  {
   "claim": "Measured fact 7 / P0 row 8 expected / P5a: on the all-filleted F1 the HLR `VCompound` is EMPTY and visible lines live only in `Rg1LineVCompound`/`OutLineVCompound` ('all-filleted F1 front view non-empty' is the VCompound-trap test).",
   "refuted": true,
   "evidence": "All 15 unique edges of F1 filleted with `BRepFilletAPI_MakeFillet.Add(r, e)` (IsDone True, 29 faces): r=1 front view: VCompound 9, Rg1LineV 17, OutLineV 0 | HCompound 14, Rg1LineH 19, OutLineH 3; r=2 front: VCompound 8, Rg1LineV 16 | H 15, Rg1H 20, OutLineH 3; top r=1: VCompound 9, Rg1LineV 17. VCompound is never empty on F1, so the P5a test as written passes without exercising the trap. The 'empty' observation comes from W3 \u00a71.3 (reports/a86e91e106b14b031.md:7) on a DIFFERENT part (120x80x10 plate, 12 holes, 96 fillets).",
   "fix": "Keep the union-of-three-visible-compounds design, but fix the fixture and the assertion: either use W3's plate (where VCompound was measured empty) as the trap fixture, or on all-filleted F1 assert `len(Rg1LineVCompound) > 0` and `visible_union (26) > len(VCompound) (9)` and that a VCompound-only renderer drops >= 17 edges. P0 row 8's expected cell becomes 'per-compound counts recorded', not 'VCompound empty'."
  },
  {
   "claim": "P0 row 7 / risk register: F5 (100 \u00d88 holes in a 220x220x12 plate) should be ONE n-ary `BRepAlgoAPI_Cut` with `SetGlue` and `SimplifyResult`, expected < 0.2 s with the same volume, and the edge count is to be pinned 'under n-ary + SimplifyResult'.",
   "refuted": true,
   "evidence": "Measured: sequential 100 cuts 0.521 s, 520,481.421 mm3, 106 faces, 624 explorer edges. N-ary `SetArguments([plate]); SetTools([100 cylinders])`: glue Off 0.090 s -> 520,481.421 / 106 faces / 624 (identical topology); `SetGlue(BOPAlgo_GlueShift)` 0.014 s -> 580,800.000 mm3 / 6 faces / 24 edges = the UNCUT plate with `IsDone() True` (silently wrong: glue modes are only valid for arguments whose faces coincide or touch without real intersections; the cylinders intersect the plate faces); `SimplifyResult()` changes nothing (106/624 with or without it). `BRepAlgoAPI_Cut` binds no `HasErrors`/`DumpErrors`, only `IsDone`.",
   "fix": "Use the n-ary cut WITHOUT `SetGlue` (optionally `SetRunParallel(True)`/`SetUseOBB(True)`); reserve `SetGlue(BOPAlgo_GlueShift|GlueFull)` for pattern FUSES of touching copies as W3 \u00a73.24 states. Pin F5 n-ary at ~0.09 s, 520,481.421 mm3, 106 faces; drop `SimplifyResult` from the F5 pin (a no-op there; keep it for fuses with coplanar faces such as F2). Law 11 (`delta_mm3 == 0` -> `no_effect`) must apply to any glue-mode boolean."
  },
  {
   "claim": "Measured fact 3 / \u00a7V3 / D7: F5 has 106 faces / 624 edges, F1 '30 edges', and every feature diff reports `faces`/`edges` counts from these numbers.",
   "refuted": true,
   "evidence": "`TopExp_Explorer(shape, TopAbs_EDGE)` visits a shared edge once per owning face. Measured: F5 explorer 624 vs `TopExp.MapShapes_s(shape, TopAbs_EDGE, TopTools_IndexedMapOfShape()).Extent()` = 312 (= 12 box edges + 100 x 3); F1 explorer 30 vs unique 15 (faces 7, vertices 10); F2 unique 33. The probe that produced 624 (scratchpad/probe/brep_probe.py:26-29 `count()`) uses the explorer.",
   "fix": "Count sub-shapes with `TopExp.MapShapes_s` (unique) everywhere the wire reports `faces/edges/solids` (faces happen to be unique in a single solid, edges and vertices are not). Pin F5 edges = 312, F1 = 15, F2 = 33 in `tests/expected.py`, with a note that explorer counts double every shared edge (624 = 2 x 312)."
  },
  {
   "claim": "D2 / risk register: `Message_ProgressRange` is the bound cancellation hook for `BRepAlgoAPI_*` and is wired in if P0 row 17 shows an op class above 10 s.",
   "refuted": true,
   "evidence": "`BRepAlgoAPI_Cut.Build(theRange: Message_ProgressRange = ...)` IS bound, but a cancelling range needs a `Message_ProgressIndicator` whose `UserBreak()` returns True: `class Cancel(Message_ProgressIndicator): ...; Cancel()` -> `TypeError: Cancel: No constructor defined!` (abstract class, no pybind trampoline); scanning every class in `OCP.OCP` for subclasses of `Message_ProgressIndicator` -> `[]`; `Message_ProgressRange` exposes only `Close/IsActive/More/UserBreak` and a default-constructed range is inactive. The same holds for `BRepMesh_IncrementalMesh(..., theRange)`, `StlAPI_Writer.Write(..., theProgress)`, `BRepTools.Write_s(..., theProgress)`, `IGESControl_Writer.AddShape(..., theProgress)`.",
   "fix": "Strike the hook: OCP 7.9.3 offers no Python-side cancellation of a running OCCT op. Keep the sidecar deadline + kill/respawn/replay as the ONLY guard, make the `MAX_BATCH_S` prediction the pre-flight, and state in D2 that `LocalKernel` (in-process) mode has no cancellation at all, so `job: true` work must run through `SidecarKernel` even in the dev venv (or the job is documented as uncancellable)."
  },
  {
   "claim": "D6 / D1 `brep/history.py` / P0 row 15: sub-shape roles are materialised from a merged `BRepTools_History` (`Generated/Modified/IsDeleted`) built 'per feature' for extrude/revolve/sweep/loft/fillet/chamfer/draft/shell/pattern, and merged with the boolean's and `ShapeUpgrade_UnifySameDomain`'s history.",
   "refuted": true,
   "evidence": "`BRepTools_History` binds `Modified/Generated/IsRemoved/Merge(BRepTools_History)/AddGenerated/AddModified/Remove/IsSupportedType_s` and NO `IsDeleted` (that name lives on `BRepAlgoAPI_BuilderAlgo`/`BRepBuilderAPI_MakeShape`). `History()` exists only on `BRepAlgoAPI_Cut/Fuse/Common/Splitter`, `BRepFeat_MakeCylindricalHole` and `ShapeUpgrade_UnifySameDomain` (returns `BRepTools_History`); `hasattr(cls,'History')` is False for `BRepPrimAPI_MakePrism`, `BRepPrimAPI_MakeRevol`, `BRepFilletAPI_MakeFillet`, `BRepFilletAPI_MakeChamfer`, `BRepOffsetAPI_DraftAngle`, `BRepOffsetAPI_MakePipeShell`, `BRepOffsetAPI_ThruSections`, `BRepOffsetAPI_MakeThickSolid`, `BRepFeat_MakeDPrism`, `BRepBuilderAPI_Transform` (each has only per-sub-shape `Generated/Modified/IsDeleted(S)`); `BRepTools_History.__init__` binds only the no-arg ctor (the templated `(arguments, algo)` ctor is unbound). `IsSupportedType_s`: vertex/edge/face/solid True, wire/shell/compound False. P0 row 15's expectation itself holds via per-sub-shape queries: F1 fillet r2 on the 4 vertical edges -> `Generated(edge)` = 1 face each, `Modified(face)` = 1 for the six touched faces and 0 for the cylinder wall, `IsDeleted` False everywhere, faces 7 -> 11.",
   "fix": "`brep/history.py`: for every non-`BRepAlgoAPI` feature build the history by hand: `h = BRepTools_History()`; for each vertex/edge/face/solid `s` of the input (from `TopExp.MapShapes_s`): `for g in algo.Generated(s): h.AddGenerated(s, g)`; `for m in algo.Modified(s): h.AddModified(s, m)`; `if algo.IsDeleted(s): h.Remove(s)`; then `h.Merge(boolean.History())` and `h.Merge(usd.History())`. Query the merged object with `IsRemoved`, never `IsDeleted`; never feed wires/shells/compounds to it. Reword P0 row 15 to 'via `MakeFillet.Generated/Modified` per sub-shape'."
  },
  {
   "claim": "P2 acceptance / D5: taper extrude (the `taper` prop of `create extrude`, including `mode new`) is done via `BRepFeat_MakeDPrism`.",
   "refuted": true,
   "evidence": "`BRepFeat_MakeDPrism.Init(Sbase: TopoDS_Shape, Pbase: TopoDS_Face, Skface: TopoDS_Face, Angle: float, Fuse: int, Modify: bool)` requires an existing base solid and one of its faces, so it cannot create the first body of a part. The free-profile route is `LocOpe_DPrism(Spine: TopoDS_Face, Height, Angle)` (bound in `OCP.LocOpe`; what cadquery `Solid.extrudeLinear(taper=)` and build123d use). Measured on a 100x60 planar face, Height 10: +3 deg -> 59,085.191 mm3, 6 planar faces, top-face centroid z = 9.99 (= 10*cos 3 deg: `Height` is measured along the drafted wall, not vertically; the vertical-height prismoid would be 59,165.138); -3 deg (outward) -> 60,756.864 mm3 and 10 faces including 4 conical corner patches (`BRepAdaptor_Surface.GetType() == 2`, 4.11 mm2 each).",
   "fix": "Implement `taper` for `mode new`/free profiles with `LocOpe_DPrism(face, distance/cos(taper_rad), taper_rad)` if `distance` means vertical height (or document that it is wall length), and pin the P2 number from that call (inward: 6 faces; outward: 10 faces with 4 cones, which `type=plane` selectors must expect). Use `BRepFeat_MakeDPrism` only for `join`/`cut` from a face of an existing body (`Sbase`, `Pbase`), which is cadquery's `dprism` case."
  },
  {
   "claim": "P2 acceptance / W1: the selector `plate:edges(dir=Z)` resolves to 4 edges on F1 and filleting them r2 gives -34.336 mm3 and faces 7 -> 11.",
   "refuted": true,
   "evidence": "On F1 (box minus the \u00d810 hole) the straight edges with direction \u00b1Z are FIVE: the corners at (0,0), (0,60), (100,0), (100,60) and the cylinder SEAM at (55,30) (`BRep_Tool.IsClosed_s(edge, cylinderFace)` -> True, found via `TopExp.MapShapesAndAncestors_s`). Adding all five to `BRepFilletAPI_MakeFillet` with r=2 still builds (`IsDone() True`, 11 faces) because OCCT silently ignores the seam (`Generated(seam)` -> 0, `Modified(cylinder face)` -> 0). W1's `resolved: {\"plate:edges(dir=Z)\": 4}` is right only because there the fillet precedes the holes.",
   "fix": "Selectors must exclude seam edges by default (`BRep_Tool.IsClosed_s(e, f)` on any ancestor face) and report it (`resolved: 4`, note `seam edges excluded: 1`); state in P2 that `dir=Z` on F1 matches 5 raw / 4 filtered. Any fillet/chamfer edge whose `Generated(edge)` is empty must be reported as `failed`/`no_effect` for that edge (law 11) instead of being silently accepted."
  },
  {
   "claim": "Measured fact 6 / P0 row 4: `OSD_Timer` and `Message_ProgressRange` are bound, `OCP.RWObj`/`OCP.RWPly` are NOT, and all 26 named classes (`BRepTools_History TNaming_Builder HLRBRep_Algo HLRBRep_PolyAlgo RWGltf_CafWriter BRepFeat_MakeDPrism XCAFDoc_DocumentTool BRepFilletAPI_MakeFillet/_MakeChamfer BRepOffsetAPI_DraftAngle/_MakePipeShell/_ThruSections/_MakeThickSolid BRepAlgoAPI_Splitter STEPCAFControl_Writer/_Reader IntCurvesFace_ShapeIntersector BRepClass3d_SolidClassifier GeomConvert_BSplineCurveToBezierCurve ShapeUpgrade_UnifySameDomain OSD_Timer Message_ProgressRange BRepExtrema_DistShapeShape BRepBndLib IGESControl_Writer StlAPI_Writer`) import.",
   "refuted": false,
   "evidence": "All 26 classes (plus `HLRBRep_HLRToShape/PolyHLRToShape`, `RWGltf_CafReader`, `BRepFeat_MakeCylindricalHole`, `STEPControl_*`, `Interface_Static`, `TopTools_FormatVersion_VERSION_3`, `BRepMesh_IncrementalMesh`, `LocOpe_DPrism`, `BOPAlgo_Glue*`, `HLRAlgo_Projector`) import from `server/.venv` (cadquery-ocp 7.9.3.1.1, `OCP/__init__.py`); `import OCP.RWObj` and `import OCP.RWPly` -> `ModuleNotFoundError`; the RW* modules present are RWGltf, RWMesh, RWStep*, RWStl (320 OCP modules total). `cadquery-ocp-novtk`, `lib3mf`, `build123d` absent; trimesh 5.0.0, ezdxf 1.4.4, fpdf2 2.8.8 present. Unbound details worth knowing: `RWGltf_CafWriter.SetDefaultSceneName`, `HLRBRep_PolyAlgo.Angle`, `BRepAlgoAPI_*.HasErrors/DumpErrors`, `STEPCAFControl_Writer.Model`.",
   "fix": "None for the claim; P0 row 4 must still re-run the same import list on the novtk wheel as the plan says, and must not plan on `SetDefaultSceneName`, `HasErrors`, or `STEPCAFControl_Writer.Model`."
  },
  {
   "claim": "Measured fact 4 / D4: `XCAFDoc_DocumentTool.SetLengthUnit_s(doc, 0.001)` + `RWGltf_CafWriter.SetMergeFaces(True)` writes the mm plate as [0.22, 0.22, 0.012] m, one geometry, readable through `tee.assets.gltf.probe`; without `LengthUnit` a 10 mm part is 10 m.",
   "refuted": false,
   "evidence": "`SetLengthUnit_s` overload 1 is documented 'Sets value of current internal unit to the document in meter'; `RWGltf_CafWriter(TCollection_AsciiString, isBinary)`, `SetMergeFaces(bool)`, `Perform(doc, TColStd_IndexedDataMapOfStringString(), Message_ProgressRange())` all bound. scratchpad `probe/plate_mm.glb` (written by `probe/brep_probe.py:55-59` with LengthUnit 0.001) reads via trimesh as 1 geometry, extents [0.22, 0.22, 0.012]; `tee.assets.gltf.probe(Path(...))` -> `{'format': 'glb', 'units': 'm', 'triangles': 10812, 'meshes': 1, 'extents_m': [0.22, 0.22, 0.012]}` (keys: format, units, triangles, meshes, materials, textures, extents_m, dims_zup_m). Trap: `probe` requires a `pathlib.Path`; a `str` raises `AttributeError: 'str' object has no attribute 'suffix'` at `server/src/tee/assets/gltf.py:182`. The no-LengthUnit negative case is W3 \u00a79.48 (cadquery never sets it) and the plan already carries it as a negative fixture.",
   "fix": "None for the mechanism; pass a `Path` to `tee.assets.gltf.probe` and keep the negative fixture (P0 row 13 measures it)."
  },
  {
   "claim": "D3/D4/D1 signatures and fixture facts: `BRepTools.Write_s(shape, path, False, False, TopTools_FormatVersion_VERSION_3)`; `BRepMesh_IncrementalMesh(shape, defl, relative=False, 0.5, InParallel=True)`; tessellation SHA-256 identical serial vs parallel (fact 8); `BRepFeat_MakeCylindricalHole` has no counterbore/countersink (fact 6); `BRepTools_History` tracks vertex/edge/face/solid only (fact 6); F2 = 13 faces (unified) / 44,916.967 mm3; fillet r=12 on F1's top-front edge refuses with `NbFaultyContours`.",
   "refuted": false,
   "evidence": "`BRepTools.Write_s` overload 4 = `(theShape, theFile: str, theWithTriangles: bool, theWithNormals: bool, theVersion: TopTools_FormatVersion, theProgress=...) -> bool` (`TopTools_FormatVersion_VERSION_3` is exposed both as `OCP.TopTools.TopTools_FormatVersion_VERSION_3` and on the enum); `BRepMesh_IncrementalMesh.__init__` overload 2 = `(theShape, theLinDeflection, isRelative=False, theAngDeflection=0.5, isInParallel=False)`; SHA-256 over all nodes+triangles of F5 at 0.05 and 0.3 mm identical for serial and 3x `isInParallel=True` (`115a72d6f9dbe8cf`, `b999ee27d66f1f33`); `BRepFeat_MakeCylindricalHole` binds only `Perform/PerformThruNext/PerformUntilEnd/PerformBlind/Init/Build/Status`; `BRepTools_History.IsSupportedType_s`: vertex/edge/face/solid True, wire/shell/compound False; F2 (base 80x60x6 + upright 80x6x34 at y 0..6) raw `BRepAlgoAPI_Fuse` = 11 faces -> `ShapeUpgrade_UnifySameDomain(s, True, True, False)` = 8 (and `BRepAlgoAPI_Fuse.SimplifyResult()` = 8) -> inner fillet r6 = 9 faces, 45,738.053 -> 4 x \u00d86.6 = 13 faces, 33 unique edges, 44,916.967 mm3; `BRepFilletAPI_MakeFillet.Add(12.0, top-front edge).Build()` -> `IsDone() False`, `NbFaultyContours() == 1`. Also: `BRepOffsetAPI_MakeThickSolid` has only the no-arg ctor plus `MakeThickSolidByJoin(S, ClosingFaces, Offset, Tol, ...)`/`MakeThickSolidBySimple(S, Offset)`; `BRepBndLib` statics are `AddOptimal_s/Add_s/AddClose_s`; `IGESControl_Writer(theUnit: str, theModecr: int = 0)`; `STEPCAFControl_Reader.ReadFile/Transfer(doc)/SetNameMode` bound.",
   "fix": "None; record that the F2 face pin (13) depends on `ShapeUpgrade_UnifySameDomain` or `SimplifyResult` (raw fuse would give 16), write the static as `BRepBndLib.AddOptimal_s`, and implement `shell` through `MakeThickSolidByJoin`."
  }
 ]
}