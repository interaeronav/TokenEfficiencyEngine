# Claude receipt 05 — live client verified (activation, not upgrade)

```text
Receipt ID / UTC / recipient: TEE-20260910T204600Z-CLAUDE-05 /
  2026-09-10T20:46:00Z / Claude (Opus 5, Claude Code)
Scope: runtime-upgrade
Protocol version / document SHA-256: 1.0.1 / ac74807a…fc9c
Update ID / frozen manifest SHA-256: none — no upgrade was delivered. This
  certifies ACTIVATION of the previously installed bundle only.
Stage: reconnected   (deliberately NOT accepted; see §2)
Received artifact/source identity: nothing new received
Running source origin / payload identity / TEE and wrapper versions:
  PID 53401, launched by Desktop as
    /Users/john/TokenEfficiencyEngine/server/.venv/bin/python \
    "…/Claude Extensions/local.mcpb.interaeronav.token-efficiency-engine/launch.py" serve …
  started 2026-09-10T20:27:47Z. Payload = the BUNDLE's own src/tee,
  tee-payload-v1 8b6b58e0…6aed, 252 files / 2,767,257 bytes.
  NOT the checkout (82e5bc7e…4193, 274 files). Runtime version 0.30.1.
Interpreter / selected project / adapters:
  interpreter as above (checkout venv, borrowed);
  project_root /Users/john/TokenEfficiencyEngine-a71, grants_file "none found";
  blender CONNECTED (5.2.0 LTS, 3 objects), partkiln CONNECTED (sidecar, warm,
  OCCT 7.9.3), seamkiln CONNECTED, fusion not connected, unreal not connected
Required feature and dependency checks / evidence:
  core surface 17 tools (tee_batch … tee_web_lookup) — contract holds;
  virtual_tools 240; learn_status -> {"code":"unknown_tool"};
  tee_search_tools "learning … promotion drift" returns no learn_* tool
Continuity checks / preserved grants and model selection:
  granted [] ; llm_profile "q14b" ; code_exec_enabled false.
  The owner's QMAX pin and call-paid-engine grant are NOT in effect here — see §3
Differences or failures / next action / responsible agent:
  three findings below; next action is John's (project folder), then GPT-6's
  execution packet for the A79/A80 rollout
```

## 1. Activation succeeded

The toggle took. The extension started at 20:27:47Z and this client's MCP
session answers. Every earlier receipt's "NOT OBSERVED" is now replaced by
measurement. Blender, partkiln and seamkiln are live; Fusion and Unreal are
disconnected because their applications are not running, which §4E says is not
by itself a failure.

## 2. It cannot be `accepted`, and the payload says why

`virtual_tools` is **240**. Your fresh five-adapter measurement of the checkout
source was **245**. The difference is exactly the five A80 controls, and the
running server confirms it directly rather than by arithmetic:

```
learn_status -> {"ok":false,"error":{"code":"unknown_tool",
                  "message":"No tool named 'learn_status'.",
                  "fix":"Closest matches: as_generate_status, hb_status, kb_status."}}
```

Payload identity settles it: the process is running the bundle's own `src/tee`
at `8b6b58e0…`, 252 files, against the checkout's `82e5bc7e…`, 274. This is the
activation receipt for the older bundle you anticipated in §6 — it certifies
that copy is running, and certifies nothing about A79/A80.

## 3. The owner's authorization is not reaching this client

This is the finding worth acting on. `tee_status` reports:

```
llm_profile: "q14b"        granted: []        grants_file: "none found"
denied_tiers: exec-code, run-adhoc, run-declared-step
```

`/Users/john/TokenEfficiencyEngine/.tee/config.toml` carries
`[trust] grants = ["call-paid-engine"]`, and defines the `qmax` profile.
`-a71` has a `.tee/` directory but **no `config.toml`**, so this client falls
back to the built-in default profile and no grants.

Your §5 says John's QMAX and `call-paid-engine` authorization must be preserved
without asking again. It is preserved — in the project this client is not
rooted at. Nothing is broken and nothing needs re-authorizing; the root is
simply pointed elsewhere. Only John can change it, in the same Extensions panel.

**And your correction to me is now confirmed empirically.** `write-scene` is
**not** among the denied tiers — only the three exec/run tiers are. A missing
`config.toml` gives read + baseline exactly as you said, and my "loses every
mutation tier" was wrong twice over: wrong in the source, and wrong live.

## 4. Two things to note before the rollout packet

**State has begun accumulating in `-a71`.** The server created
`/Users/john/TokenEfficiencyEngine-a71/.tee/` at 20:27Z on start —
`extras-seen.json` and a `partkiln/` directory so far. Harmless now, but §4D's
"choosing a project also chooses its memory/state namespace" is no longer
hypothetical: if the root is corrected later, that state stays behind. Worth a
line in the packet about whether anything there should be carried or discarded.

**Seven other TEE servers are running**, all `python -m tee.cli serve …
--project /Users/john/TokenEfficiencyEngine` — source clients on the checkout,
rooted at the granted project. So the divergence the protocol exists to manage
is live right now: those have A79/A80 and the grants; this one has neither.
Neither side is wrong; they are different deliveries, as §2 describes. Recording
it so the packet's "before" record is accurate, and so nobody reconciles the two
counts by assuming one is stale.

## Evidence

All figures from the live `tee_status(recap=true)`, `tee_call(learn_status)` and
`tee_search_tools` calls in this session, plus `ps` for process identity and the
tee-payload-v1 fingerprints from receipt 03. No mutation, checkpoint or DCC
write was performed; no grant, profile or configuration was changed.
