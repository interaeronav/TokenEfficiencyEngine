# Frozen HF1 delivery execution

This is the current execution record for TEE-20260911-A82-HF1 revision 1.
GPT-6 has reviewed and frozen the artifacts and common packet; preparation
evidence in execution.md still defines the exact checks and rollback procedure.
The earlier pending-freeze statements in draft-era files are historical.

1. Review the final release-manifest.json and verify its separate checksum.
   Verify every common file, both source/member identities and the appropriate
   recipient artifact. The local verifier VERIFY_FROZEN.py accepts the delivered
   recipient directory as its argument and writes nothing.
2. Claude returns a received/review receipt naming the exact frozen manifest.
   This precedes any rollout because GPT-6 built both artifacts. Codex's review
   is separately recorded. No new owner grant is required.
3. GPT-6 refreshes continuity and verifies the entire served checkout still
   matches accepted A82. Preserve the old backends.py privately and apply only
   its reviewed HF1 replacement; verify the complete resulting payload.
   Reconnect the actual Codex TEE client. Do not extract the full ZIP over owner work.
4. Install the delivered Claude local MCPB using supported Desktop controls,
   keeping /Users/john/TokenEfficiencyEngine and observing enabled state.
   Reconnect the actual Claude TEE client. Restarting the old package is insufficient.
5. Both clients return actual runtime acceptance receipts with origin, full
   payload, interpreter, project, grants/QMAX, tools, dependencies and continuity.
   Import/inspect the fixed backend without a paid task or opening a browser.
   Harness probes do not substitute for either receipt. GPT-6 closes the ledger
   only once both receipts match this manifest.

Rollback is the exact accepted A82 artifact in each recipient's rollback folder;
selective source restoration must preserve later owner edits. There is no data
migration and no dependency change. Preserve new memory, learning, documents,
both approved grants and QMAX. Retain accepted sets under protocol section 3.

The fix is for TEE-managed Aider Python browser offers on this verified macOS
installation. It preserves printed metadata and credential warnings. It does not
install into unrelated standalone Aider sessions, change browser preferences,
claim QMAX token/cost metadata, or complete the ongoing A83 architecture work.
