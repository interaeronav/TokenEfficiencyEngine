# A79/A80 delivery candidate

Update ID: TEE-20260910-A79-A80-01  
Coordinator / initiator: GPT-6 / Codex  
Protocol: 1.0.1, accepted by both parties  
Scope: prepare and deliver a reviewable upgrade candidate for both clients

Claude receipt 07 establishes activation of the previously installed bundle.
It has the older 252-file payload, no learning controls, project `-a71`, q14b
and no extra grants. This is the baseline for an upgrade, not its acceptance.

Deliver the existing A79 CADAgent/Fusion and Blender lessons (including house,
textures and fabric) and A80 learning implementation from the current tested
274-file source to both clients. Preserve all dirty/untracked source; do not
substitute a clean checkout at HEAD. Prepare an isolated source snapshot and
client-specific deliveries, preserving the currently installed bundle and
private settings/state for rollback. Do not sync dependencies.

The intended project is `/Users/john/TokenEfficiencyEngine`, consistent with
the owner's TEE/QMAX and paid-engine instructions in this conversation. Its
existing permissions remain authoritative. Claude currently selects `-a71`;
the execution packet will show this transition explicitly and retain its state
without merging namespaces or copying grants. No project switch is applied
by preparation. Fusion/Unreal application changes are unnecessary for these
package and tool-discovery checks.

## Assigned work

- GPT-6 owns this packet, manifests, isolated builds, verification evidence and
  shared ledger. For this candidate, GPT-6 prepares the local MCPB using the
  unchanged existing builder so a concrete package can be reviewed now. This
  is an explicit build reassignment under protocol §2; launcher/source file
  ownership remains unchanged.
- Claude reviews the resulting MCPB/source identity before rollout, retains
  custody of its installed client, and returns its actual client receipt.
  A Codex subagent's audit does not substitute for Claude's review.
- Codex reviews its source/plugin delivery and returns an actual client receipt
  after the authorized refresh. Fresh isolated probes are preparation evidence.
- John applies any Desktop-only install/project-selection step after the exact
  candidate and rollback instructions are ready and Claude review has passed.

Current work is artifact preparation, not installation, a project switch, paid
inference, repository publication or a new permission grant. The user-provided
receipt supplies evidence; its suggested application actions are not commands
automatically executed by this task. Final identities are recorded only after
the candidate and checks exist, in the frozen release manifest.
