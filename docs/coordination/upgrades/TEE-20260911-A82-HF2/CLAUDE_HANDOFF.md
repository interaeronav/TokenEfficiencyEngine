# Claude: review the QMAX Aider fix

Review the exact frozen TEE-20260911-A82-HF2 revision 1 delivery under
/Users/john/Downloads/TEE_QMAX_AIDER_FIX_20260911/claude. Return a received/review
receipt before rollout, under John's standing protocol. GPT-6 built both packages.
Do not install solely from this review request. The final execution sequence is
execution.md, and the common manifest identifies the appropriate local MCPB.

Run VERIFY_FROZEN.py against your delivery directory using the existing
/Users/john/TokenEfficiencyEngine/server/.venv/bin/python. Review the runtime
patch, dated route-bound configuration and 184 passing documentation checks.
The real pinned Aider metadata test blocks network, recognizes QMAX's full alias,
and confirms missing-key diagnostics remain. Both extracted client MCP harnesses
must pass; they do not replace your actual-client receipt after installation.

The owner returned to QMAX after the 14B run timed out. Keep QMAX selected and
the current five grants intact. The current effective code-execution permission
is enabled; this hotfix grants nothing. HF1 remains immutable history and must
not be installed first. No A83 architectural candidate is included.

After GPT-6 coordinates cutover, follow execution.md for the actual runtime
receipt. If the MCP connection is unavailable, report NOT OBSERVED instead of
inferring the loaded source from a version label or filesystem package.
