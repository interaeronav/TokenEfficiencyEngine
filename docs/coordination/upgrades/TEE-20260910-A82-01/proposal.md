# A82 combined architecture and documentation update

Update **TEE-20260910-A82-01**, revision 1. GPT-6 coordinates under accepted
protocol 1.0.1. The owner requested the architectural application after A81 was
prepared, then directed quiet autonomous continuation while asleep. This is a
new combined candidate, retaining A81's immutable held delivery and the accepted
A79/A80 rollback. It does not reinterpret either earlier manifest.

Prepare the complete current runtime, including A81's five documentation-worker
tools and A82's eleven architectural tools/resources, from one isolated snapshot.
The source manifest must include every shipped runtime/resource file, including
dirty and untracked work. No clean/reset/stash/blanket commit or publication.
Final source identity, artifact hashes and measured checks are recorded in the
frozen release manifest; version labels alone do not identify this candidate.

GPT-6 owns both builds, common packet, continuity checks and ledger. Use the
unchanged local MCPB builder for Claude and the same runtime with the verified
existing Codex source wrapper for Codex. Claude owns its other-party review and
actual-client receipt. Both actual-client receipts are required to close rollout.

Architecture provides original millimetre documents, stable per-document/entity
identity, supported L/T wall joins, openings, spaces, slabs, flat roofs, cabinets,
IFC/GLB/drawings, measured reference conversion, rule-pack assessment and optional
authenticated local GUI. Worldwide law coverage is an extensible framework;
there is no complete verified global code corpus or legal certification. Detailed
construction systems, live Unreal exporter acceptance and manufacturer/CNC
approval remain outside the demonstrated initial vocabulary. Read the guide and
evidence before describing capabilities to stakeholders.

Preserve main project `/Users/john/TokenEfficiencyEngine`, QMAX pin/readiness,
the existing sole `call-paid-engine` grant, code execution disabled, both state
namespaces, learning and previous lesson tools. No new shared dependency is
required. Cline/Aider remain separate optional installations. `run-doc-agent`
still needs the owner's separate decision and cannot be added while an older
runtime that rejects it remains active. Architecture adds no capability grant.

Build/review/deliver quietly. Do not change actual clients or seek an owner UI
action while the owner sleeps. Save a Claude-ready file in Downloads; send no
external message. Prepared artifacts, fixture probes and one client's success
cannot substitute for installation and both actual-client receipts.
