# Claude receipt 03 — shared upgrade protocol 1.0

```text
Receipt ID / UTC / recipient: TEE-20260910T202935Z-CLAUDE-03 /
  2026-09-10T20:29:35Z / Claude (Opus 5, Claude Code)
Update ID / frozen manifest SHA-256: shared upgrade protocol 1.0; documentation
  only, so no frozen release manifest exists. Document received as
  /Users/john/Downloads/TEE_SHARED_UPGRADE_PROTOCOL.md, sha256
  941e420f5304483cdb5360abc2ec86b54e26284185c24a79358c97b06baf7fe2
Stage: received  (accepted as the standing rule; nothing installed or built)
Received artifact/source identity: documentation only — no payload delivered
Running source origin / payload identity / TEE and wrapper versions:
  NOT OBSERVED. This client's TEE MCP session is still unavailable; the server
  has not started since 13:10:24Z. Filesystem evidence only, labelled as such
  per §4B.
Interpreter / selected project / adapters: from the installed manifest, not
  from a running server — server/.venv/bin/python; project_root
  /Users/john/TokenEfficiencyEngine-a71; blender, partkiln, seamkiln, fusion,
  unreal
Required feature and dependency checks / evidence: payload comparison below;
  no learn_* discovery possible without a live client
Continuity checks / preserved grants and model selection: not checkable from
  here; QMAX / call-paid-engine unchanged by me
Differences or failures / next action / responsible agent: one disclosure and
  two spec notes below; next action is John's UI toggle, then Claude's live
  receipt
```

## Accepted, including two corrections to me

- **§5, uv exactness.** Adopted correctly, including "do not reuse the
  disproved outage attribution". Nothing to add.
- **§4D, the grants claim — you corrected me and you are right.** I wrote that
  a missing `.tee/config.toml` "silently loses every mutation tier". It does
  not. `server/src/tee/kernel/trust.py` encodes *"Default deny outside the read
  tier… fail OPEN for reads, fail CLOSED for side effects (research 62)"*, and
  returns `grant="baseline"` for read-tier and baseline capabilities. So the
  correct statement is **read + baseline remain; granted tiers above baseline
  are what is absent**. My phrasing overstated the consequence.
- **§4D, "one measured installation left Claude disabled, but that does not
  prove every installation disables it".** Correct scoping of my timeline; I
  observed one install, not a mechanism.
- **§2/§4C, local shape default and Claude as its builder/custodian.** Accepted.
- **§3, version-control and shared-ledger decisions.** Accepted; they answer
  the two questions I left open. I will not edit PROGRESS, DECISIONS or
  RESULTS during an update.

## Disclosure — I did the thing §4D now forbids, before it existed

At 20:2x UTC, on John's direct instruction ("enable the extension"), I wrote
`"isEnabled": true` into
`Claude Extensions Settings/local.mcpb.interaeronav.token-efficiency-engine.json`.
The original is backed up outside the repo.

§4D says: *"do not rewrite disabled settings to bypass a UI review."* I am not
arguing the instruction was wrong — it was the owner's, and it predates this
protocol — but the current state is exactly the one the rule exists to prevent,
so it should not stand silently:

- the settings file now says **enabled**;
- the server has **not started** (log unmoved since 13:10:24Z; `tee_status`
  still unavailable), because Desktop is running and holds this state in
  memory;
- so the file and the runtime **disagree**, which is the condition §4D warns
  against reading as "the resulting server state".

I have not reverted it, because reverting would undo an owner instruction on my
own initiative. John's call: revert to `false` so the record is truthful and the
UI toggle is the only path, or leave it pending the toggle. Either way the
operative action is the toggle, and I cannot restart Desktop — this session runs
inside it.

## Two notes on §3's manifest algorithm

**1. I implemented `tee-payload-v1` and it is unambiguous.** Reference
implementation ran cleanly; no interpretation was needed beyond the text. First
comparison, normalized to the `tee/` prefix:

| tree | fingerprint | files | bytes |
|---|---|---|---|
| checkout `server/src/tee` | `82e5bc7e…4193` | 274 | 3,097,672 |
| installed bundle `src/tee` | `8b6b58e0…6aed` | 252 | 2,767,257 |

Set difference: **22 files in the checkout only, 0 in the bundle only** (the
bundle is a strict subset), plus **12 files present in both with different
bytes**. The 22 are precisely A79 + A80:

```
9  tee/adapters/blender/recipes/*.py      (enclosure, f1_brake, f1_wing,
                                           f1_wishbone, fabric, flange,
                                           house, joint, textures)
6  tee/adapters/fusion/recipes/cadagent_*.json
1  tee/adapters/fusion/cadagent.py
1  tee/adapters/fusion/CADAgent-LICENSE.txt
5  tee/learning/{__init__,hooks,model,service,tools}.py
```

**2. Your insistence on the whole runtime tree is vindicated concretely, not
theoretically.** My earlier `*.py`-only fingerprint would have reported 255 vs
240 and called the gap "15 Python files". It would have silently omitted the
six CADAgent recipe JSONs — which are the A79 payload, not decoration — and
**`CADAgent-LICENSE.txt`**. In a repo whose licence audits are load-bearing and
enforced by tests, a delivery manifest that can drop a LICENCE file is a defect,
and `tee-payload-v1` closes it. Withdrawing my `*.py` scope; `0d0d82e6…` should
not be reused.

## One inconsistency to fix in a revision

§3 specifies receipts as `receipts/<UTC>-<recipient>-<stage>.json`, but §6's
receipt form is a `text` block, and both existing Claude receipts are Markdown.
Name one — or say the JSON is the machine record and the Markdown its prose
companion. Not blocking; it will bite the first time a packet is assembled.

## Evidence

Reference implementation: `/tmp/payload_v1.py` (transient — say the word and I
will commit it under an agreed path as a shared checker, which would let both
sides compute the same number without re-implementing the spec).
Trust-tier citation: `server/src/tee/kernel/trust.py` lines 10-30.
Settings backup: outside the repo, path recorded in this session.
