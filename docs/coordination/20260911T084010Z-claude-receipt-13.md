# Claude receipt 13 — runtime acceptance, TEE-20260911-A82-HF2 rev 1

```text
Receipt ID / UTC / recipient: TEE-20260911T084010Z-CLAUDE-13 / 2026-09-11T08:40:10Z /
  Claude (Fable 5.1, Claude Code) — actual installed client
Scope: runtime-upgrade
Protocol version / document SHA-256: 1.0.1 / ac74807a…fc9c
Update ID / frozen manifest SHA-256: TEE-20260911-A82-HF2 revision 1 /
  68fe44947e9ff174678dbfed1d3b581171ef2e4bd56c4f9fd93e16413f507d26
Stage: accepted

Received artifact/source identity:
  tee-engine-0.30.1-a82-hf2-local.mcpb, sha256
  1bd87459cddd5e86641f806e8dae09236aef232322725bb853cd1ba4d2757ca9,
  1,141,293 bytes — re-hashed before the handover copy and again on the copy

Running source origin / payload identity / TEE and wrapper versions:
  PIDs 8845/8861, started 2026-09-11T08:37:38Z — FRESH, replacing 92918/93029
  (the 07:00Z app-update restart that had relaunched A82). Source written
  08:37:17Z. Launcher: checkout venv python + the bundle's own launch.py, five
  adapters, PYTHONDONTWRITEBYTECODE=1.
  Installed payload: tee-payload-v1
    c0f95e836b4207f3fa8bfe45f3d9c8781e622025d8027841d0b27bcac096811e
    294 files / 3,435,983 bytes — IDENTICAL to the frozen source manifest and
    to the live checkout after GPT-6's 08:28Z cutover.
  302 non-cache members (301 + docagents/model_metadata.py). TEE 0.30.1.

Interpreter / selected project / adapters:
  checkout venv (borrowed); project_root /Users/john/TokenEfficiencyEngine,
  grants_file present; blender, partkiln, seamkiln, fusion, unreal

Required feature and dependency checks / evidence:
  core 17; virtual_tools 261 WITH the main corpus (HF2 adds no tools, as
  expected); five doc_* discovered on the new payload; doc_status: aider
  0.86.2 + cline 3.0.61 version_verified, model qmax paid ready,
  execution_allowed TRUE (run-doc-agent granted); 148/148 distributions,
  no drift, 1,339 constraints satisfied; sidecar 10

Continuity checks / preserved grants and model selection:
  qmax pinned (file: active qmax, pinned true, re-pinned 08:29Z); grants
  call-paid-engine, exec-code, run-adhoc, run-declared-step, run-doc-agent —
  all five preserved; code_exec_enabled true; jobs [] / checkpoints [];
  tee_recall coherent; learn_status healthy (198 events, no promotion);
  ak_status unchanged; fresh Blender read taken

Differences or failures / next action / responsible agent:
  no failing check. GPT-6 reconciles with Codex's HF2 receipt and closes.
```

## The hotfix, observed on the actual client

**Metadata table identity.** The rooted config (`grants_file` above) carries
`[llm.profiles.qmax.docagent_metadata]`, verified at 08:33Z **semantically
identical** to the delivered JSON whose sha256 is `d158cc6d…`; the parent
profile still names `claude-qwen-max`, `http://127.0.0.1:4000/v1`, `paid=true`;
the candidate's own `aider_metadata()` accepts it with no network (max_input
983,616, request 2,048). Verified without printing the private config.

**`doc_status` does not print the table** — expected; the handoff records that
public `doc_status` does not yet expose it. What it does show is the change
that matters: `execution_allowed: true` on `run-doc-agent`, with both workers
version-verified and QMAX ready. No worker was launched and no paid request
made; GPT-6 owns the bounded generation check.

**Browser blocker precondition holds on this Mac:** `/usr/bin/true` is uid 0,
mode 0755, regular, executable — passes the patch's exact predicate (08:20Z).

**Core schema:** as before, this connector exposes no raw `tools/list` JSON;
receipt 12 reproduced the 17 core schemas from this exact payload against the
delivered file. The payload running is that payload.

## Continuity, stated precisely

- Learning: events 151→198 through ordinary use; an evaluation ran (154/16),
  candidate Brier 0.115 vs baseline 0.112, promotion **refused** on
  `insufficient_coverage` + `no_reliability_gain`. Guard working; no model.
- Blender: the scene is now **one mesh, `C208Prop` (id `b921`)** — owner work
  since the 13-object house. The house is preserved in GPT-6's durable
  checkpoint `blender-before-hf2.blend` (109,896 B, sha256 `40c31610…`,
  verified on disk 08:33Z). IDs were reissued on reconnect; nothing was
  overwritten to recreate an old ID.
- Fusion: connected, 2705.1.15, empty "Untitled" parametric design. Unreal:
  editor running, no MCP listener. Neither is in this hotfix's scope.
- `-a71` untouched. Old cancelled Aider jobs not revived.

## Two records to update at closure (GPT-6's ledger)

1. Memory fact `aider_qmax_fix` still says "Claude reconnect and runtime
   acceptance pending" — superseded by this receipt.
2. The metadata table expires **2026-09-25**; after that every Aider/QMAX doc
   run fails closed with `docagent_model_metadata`. Recorded in receipt 12 and
   the handoff; worth a dated reminder in PROGRESS since `doc_status` cannot
   yet show it.

Rollback remains genuine: the delivered `rollback/` artifact is the exact A82
installer (`98f5116f…`) this client ran until 08:37Z.
