# Claude receipt 07 — consolidated live client state

Supersedes the state descriptions in receipt 05 (whose Fusion/Unreal cause was
wrong) and folds in receipt 06's correction. Both remain valid historical
records per §6 and are not rewritten. This is the single current record.

```text
Receipt ID / UTC / recipient: TEE-20260910T205119Z-CLAUDE-07 /
  2026-09-10T20:51:19Z / Claude (Opus 5, Claude Code)
Scope: runtime-upgrade
Protocol version / document SHA-256: 1.0.1 /
  ac74807ac0ce9ea29c070e8fe57cdd84c2952182234cd61337d0b1446f85fc9c
Update ID / frozen manifest SHA-256: none — no upgrade delivered. This records
  ACTIVATION of the previously installed bundle.
Stage: reconnected   (not accepted; the A79/A80 payload is absent — see §3)

Received artifact/source identity: nothing new received

Running source origin / payload identity / TEE and wrapper versions:
  PID 53401, started 2026-09-10T20:27:47Z, launched by Desktop as
    /Users/john/TokenEfficiencyEngine/server/.venv/bin/python \
    ".../Claude Extensions/local.mcpb.interaeronav.token-efficiency-engine/launch.py" \
    serve --adapter blender --adapter partkiln --adapter seamkiln \
          --adapter fusion --adapter unreal --project <user_config>
  Payload: the BUNDLE's own src/tee — tee-payload-v1
    8b6b58e0cafa4ea7e9ed2696b85b3979432bd849adf3ceac67c7a8a904be6aed
    252 files / 2,767,257 bytes
  NOT the checkout: 82e5bc7e57f5802dc8f2aea912d4a4d389272bb50a512e45768559c647ab4193
    274 files / 3,097,672 bytes
  Runtime package version 0.30.1. Extension settings sha256
    363ef94631840355d8b773cb4d036befac07aeab32e65c53ff716bfbb1ab7a87
    (isEnabled true, project_root /Users/john/TokenEfficiencyEngine-a71)

Interpreter / selected project / adapters:
  interpreter: the checkout's server/.venv/bin/python (borrowed, not owned)
  project_root: /Users/john/TokenEfficiencyEngine-a71 — grants_file "none found"
  adapters served: blender, partkiln, seamkiln, fusion, unreal

Required feature and dependency checks / evidence:
  core surface 17 tools — contract holds
  virtual_tools 240 (this five-adapter composition, two DCC lanes unconnected)
  learn_status -> {"code":"unknown_tool"}; no learn_* tool discoverable
  live read: tee_scene_summary(blender) -> 3 entities, b115/b116/b117

Continuity checks / preserved grants and model selection:
  granted [] ; llm_profile "q14b" ; code_exec_enabled false
  denied_tiers: exec-code, run-adhoc, run-declared-step (1 tool each)
  write-scene is NOT denied — read+baseline as §4D states
  The owner's QMAX pin and call-paid-engine grant are NOT in effect here (§4)

Differences or failures / next action / responsible agent:
  three owner actions and one GPT-6 action, listed in §5
```

## 1. Lane state, measured — not inferred from a flag

| Lane | `connected` | Application | Bridge | Reality |
|---|---|---|---|---|
| blender | **true** | running, GUI (`background:false`) | — | serving; live read returns Camera/Cube/Light from 5.2.0 LTS |
| partkiln | **true** | n/a (headless) | sidecar warm | OCCT 7.9.3 |
| seamkiln | **true** | n/a (headless) | in-process | cotton_jersey default |
| fusion | **false** | **RUNNING** | **:8766 listening** | **no active design** — not a connection fault |
| unreal | **false** | **RUNNING** (UE_5.8) | **:8000 nothing listening** | Epic's MCP server not started |

`fu_probe` gives the Fusion detail directly: `connected:true, port 8766,
version 2705.1.15, document null, design null`.

Receipt 05 said both applications were not running. They are. I inferred a
cause from a boolean instead of measuring it; receipt 06 recorded the
correction and this table replaces it.

## 2. Fusion version fact

Running Fusion is **2705.1.15**. `CLAUDE.md` and doc 71 §3 record the lane's
live verification against **2704.1.53** (A71). Nothing here contradicts those
rows; they are simply one release behind their evidence. The packet's "before"
record should carry the running build.

## 3. Why this is `reconnected` and not `accepted`

`virtual_tools` is **240**; the checkout composition measures **245**. The
difference is the five A80 controls, confirmed at the tool boundary rather than
by arithmetic — `learn_status` returns `unknown_tool`. The payload fingerprints
settle it: 252 files running against the checkout's 274, a gap of 22 files that
is exactly A79's nine Blender recipes, six CADAgent JSON recipes, `cadagent.py`,
`CADAgent-LICENSE.txt`, and A80's five `learning/` modules.

Activation of the older bundle is proved. A79/A80 delivery is not, and §6 says
it cannot be certified by this receipt.

## 4. The owner's authorization is not reaching this client

`llm_profile "q14b"`, `granted []`, `grants_file "none found"`.
`/Users/john/TokenEfficiencyEngine/.tee/config.toml` carries
`[trust] grants = ["call-paid-engine"]` and defines `qmax`; `-a71` has a `.tee/`
directory but no `config.toml`.

Nothing is broken and no re-authorization is needed — §5's decision is
preserved in the project this client is not rooted at. The server has also begun
writing state into `-a71` (`.tee/extras-seen.json`, `.tee/partkiln/`, created
20:27Z), so the namespace is populating there; if the root is corrected, that
state stays behind.

## 5. Open items

**John (three, all inside applications):**
1. set the extension's project folder to `/Users/john/TokenEfficiencyEngine` if
   QMAX and the paid-engine grant should apply here;
2. open or create a Fusion design, if a live `fu_*` check is wanted;
3. start Epic's MCP server in the running editor
   (`-ModelContextProtocolStartServer` or its project setting), if a live `ue_*`
   check is wanted.

None is required for the current state to be valid; each only widens what a
rollout could verify.

**GPT-6 (one):** the A79/A80 execution packet, when initiated. Both DCC lanes
being reachable changes what that packet can verify, not what has been
delivered.

## 6. Two notes offered for the next protocol revision

- **`connected` is overloaded.** At one instant,
  `tee_status.adapters.fusion.connected` is false while `fu_probe.connected` is
  true. Both are correct and answer different questions. A receipt should name
  the probe that produced the value, or a reader comparing two receipts will
  see a lane flapping when nothing changed.
- **Keep two independent payload implementations.** Withdrawn my offer of a
  shared checker in receipt 04: two independently written implementations
  agreeing on 274/252/22/0/12 is what validates `tee-payload-v1`. One shared
  checker would replace two measurements with one and hide its own bugs.

## Evidence

Live `tee_status`, `tee_call(fu_probe)`, `tee_call(learn_status)`,
`tee_search_tools` and `tee_scene_summary(blender)` in this session; `ps`/`lsof`
for process and port identity; `shasum` for settings and payload fingerprints.
No mutation, checkpoint, DCC write, grant change, profile change or
configuration change was performed.
